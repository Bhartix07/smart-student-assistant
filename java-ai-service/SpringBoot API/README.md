# Spring Boot API (Placeholder)

This folder is intended to contain a Spring Boot project for the Java AI service.

To get started, create a standard Spring Boot project here (for example, using https://start.spring.io) and place the generated sources in this folder.

Once the project is in place, you can run it using:

```bash
./mvnw spring-boot:run
# or
./gradlew bootRun
```
